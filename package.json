{
  "name": "vi-designs-website",
  "private": true,
  "type": "module",
  "description": "Optional development checks. The website has no runtime dependencies.",
  "scripts": {
    "format": "prettier --write index.html css script/*.js tests README.md",
    "format:check": "prettier --check index.html css script/*.js tests README.md",
    "test": "node --experimental-vm-modules tests/site.cjs"
  },
  "devDependencies": {
    "html-validate": "9.7.1",
    "jsdom": "26.1.0",
    "prettier": "3.6.2"
  }
}
